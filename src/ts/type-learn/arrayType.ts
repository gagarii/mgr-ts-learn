// array type

/**
 * 配列の型を表す場合、'[]'を使う
 */
const fooArr: number[] = [0, 1, 2, 3];
fooArr.push(4);
console.log(fooArr); // [ 0, 1, 2, 3, 4 ]

const bar: string[] = ['aa', 'bb', 'cc'];
bar.push('dd');
console.log(bar); //[ 'aa', 'bb', 'cc', 'dd' ]